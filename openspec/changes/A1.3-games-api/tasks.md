# A1.3 Tasks

- [ ] Create `practice/serializers.py` with `WordQuestionSerializer`, `GameSessionSerializer`
- [ ] Create `practice/api_views.py` with all game API views
- [ ] Register routes in `speechef/api_urls.py` under `practice/`
- [ ] Ensure game session creation is wired into POST endpoints
- [ ] Add leaderboard endpoint using `GameSession` aggregation
- [ ] Test each endpoint with and without authentication
